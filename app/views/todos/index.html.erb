<div class="w-full">
  <div class="mb-6">
    <h1 class="font-bold text-4xl">Turbo Tasks</h1>
  </div>

  <%= render "form", todo: Todo.new %>

  <%= turbo_stream_from "todos" %>
  <div id="todos" class="divide-y list-none">
    <%= render @todos %>
  </div>
</div>
